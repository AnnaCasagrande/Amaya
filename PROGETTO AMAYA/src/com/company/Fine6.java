package com.company;


/*import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.layout.StackPane;
import javafx.stage.Stage;
*/
public class Fine6 extends Close {
    /*Scene scene2;
    Button bottone1, chiudi;
    public Scene intro(Stage finestra1 ){
        Close close= new Close();
        bottone1 = new Button("AVANTI");
        chiudi = new Button("EXIT");

        Label inizio = new Label("");

        chiudi.setOnAction(event -> close.closeX(finestra1));

        StackPane layout2 = new StackPane();
        layout2.getChildren().addAll(inizio, bottone1, chiudi);
        scene2 = new Scene(layout2, 700, 500);

        return scene2;
    }
*/
}

